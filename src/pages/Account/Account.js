import React from "react";
import { StyleSheet, Text, View } from "react-native";
import AccountStackNavigation from "../../navigations/AccountStackNavigation";

const Account = () => {
  return <AccountStackNavigation></AccountStackNavigation>;
};

export default Account;

// const styles = StyleSheet.create({});
